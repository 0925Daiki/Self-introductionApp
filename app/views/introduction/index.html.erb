<main class="main">
  <div class="inner">
    <% if user_signed_in? %> 
      <div class= "greeting">
        <%= link_to "Hello #{current_user.name}", "#", class: :greeting__link %>
      </div>
    <% end %> 
    <div class="card__wrapper">
    <%# <%= render partial: "prototype", collection: @prototype %> 
    </div>
  </div>
</main>
